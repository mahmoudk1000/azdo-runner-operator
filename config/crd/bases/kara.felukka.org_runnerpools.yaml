---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: runnerpools.kara.felukka.org
spec:
  group: kara.felukka.org
  names:
    kind: RunnerPool
    listKind: RunnerPoolList
    plural: runnerpools
    singular: runnerpool
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RunnerPool is the Schema for the runnerpools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              azUrl:
                pattern: ^https:\/\/[a-zA-Z0-9.-]+\/[a-zA-Z0-9_.-]+$
                type: string
              capabilities:
                additionalProperties:
                  type: string
                description: |-
                  Capabilities are passed to agents in capabilityAware mode as default
                  capabilities (e.g. {"os": "linux"}). In non-aware mode these are ignored.
                type: object
              capabilityAware:
                description: |-
                  CapabilityAware enables per-capability autoscaling.
                  When true, the operator inspects queued jobs for required capabilities
                  and deploys agents with matching images from CapabilityImages.
                type: boolean
              capabilityImages:
                additionalProperties:
                  type: string
                description: |-
                  CapabilityImages maps capability names to agent container images.
                  When capabilityAware is true, the operator creates agents with specific
                  images based on what capabilities the jobs in the queue demand.
                  Example: capabilityImages: {docker: "my-registry/agent:docker", kubernetes: "my-registry/agent:k8s"}
                type: object
              certTrustStore:
                items:
                  properties:
                    secretName:
                      type: string
                  type: object
                type: array
              extraEnv:
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                    valueFrom:
                      properties:
                        configMapKeyRef:
                          properties:
                            name:
                              type: string
                          required:
                          - name
                          type: object
                        secretKeyRef:
                          properties:
                            key:
                              type: string
                            name:
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
              image:
                pattern: ^[a-zA-Z0-9._/-]+$
                type: string
              imagePullPolicy:
                default: IfNotPresent
                enum:
                - Always
                - Never
                - IfNotPresent
                type: string
              initContainerSpec:
                properties:
                  image:
                    type: string
                type: object
              maxAgents:
                default: 5
                minimum: 1
                type: integer
              minAgents:
                default: 0
                minimum: 0
                type: integer
              patSecretKey:
                default: token
                type: string
              patSecretName:
                type: string
              pool:
                type: string
              securityContext:
                properties:
                  fsGroup:
                    format: int64
                    type: integer
                  privileged:
                    type: boolean
                  runAsGroup:
                    default: 1001
                    format: int64
                    type: integer
                  runAsUser:
                    default: 1001
                    format: int64
                    type: integer
                type: object
              storage:
                items:
                  properties:
                    claimName:
                      type: string
                    deleteWithAgents:
                      type: boolean
                    mountPath:
                      pattern: ^\/[a-zA-Z0-9\/._-]+$
                      type: string
                    name:
                      type: string
                    size:
                      pattern: ^[0-9]+([EePpTtGgMmKki]{0,2})?$
                      type: string
                    storageClass:
                      type: string
                  required:
                  - mountPath
                  - name
                  type: object
                type: array
              ttlIdleSeconds:
                default: 10
                type: integer
            required:
            - azUrl
            - image
            - patSecretKey
            - patSecretName
            - pool
            type: object
          status:
            properties:
              condition:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionStatus:
                type: string
              lastError:
                type: string
              lastPolled:
                format: date-time
                type: string
              organizationName:
                type: string
              poolName:
                type: string
              queuedJobsByCap:
                additionalProperties:
                  type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
